{
  "name": "wordflash",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm-run-all --sequential mkdir-dist copy-json parcel-dev",
    "parcel-dev": "parcel index.html",
    "copy-json": "copy json_gifs\\*.* .\\dist",
    "mkdir-dist": "IF NOT EXIST dist mkdir dist",
    "build": "parcel build index.html --out-dir ./docs --public-url ./"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "cssnano": "^4.1.10",
    "npm-run-all": "^4.1.5"
  },
  "comments": {
    "build": "for Github pages need to output to docs folder; the public-url makes sures the script src works in subdirectory (no forward slash)"
  }
}
